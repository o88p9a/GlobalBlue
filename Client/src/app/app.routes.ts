import { Routes } from '@angular/router';
import {VatCalculatorComponent} from './vat/vat-calculator/vat-calculator.component';

export const routes: Routes = [
  { path: '', component: VatCalculatorComponent },
];
